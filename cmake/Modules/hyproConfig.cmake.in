set(hypro_VERSION @hypro_VERSION@)

get_filename_component(hypro_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

@DEPENDENT_TARGETS@

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET hypro AND NOT hypro_BINARY_DIR)
  include("${hypro_CMAKE_DIR}/hyproTargets.cmake")
endif()

@PACKAGE_INIT@

set(hypro_INCLUDE_DIR "@CONF_INCLUDE_DIRS@")
set(hypro_INCLUDE_DIRS "@CONF_INCLUDES@")
set(hypro_INCLUDES "@CONF_INCLUDES@")
set(hypro_LINKER_LIBS "@CONF_LINKER_LIBS@")
set(hypro_LIBRARIES hypro)
check_required_components(hypro)
