#include "benchmarkPolytope.h"


namespace benchmark {
namespace polytope {

    Results<std::size_t> unite(const Settings& settings) {
        Results<std::size_t> res;

        return res;
    }

} // polytope
} // benchmark
