#pragma once

#include "Benchmark.h"

namespace hypro {

/*
template<typename Representation, typename Number, int operation>
template<typename Other>
BenchmarkGenerator<Other,Number,operation> BenchmarkGenerator<Representation, Number,
operation>::convertRepresentation() const { BenchmarkGenerator<Other,Number,operation> res;

auto& oldGenerator = this->getGenerator();

res.setSetup(mSetup);
}
*/

}  // namespace hypro
